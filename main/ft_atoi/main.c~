
#include <stdlib.h>
#include <stdio.h>

int main(int argc, char **argv)
{
  if (argc > 1)
    {
      printf("la vrai fonction : %d \n", atoi(argv[1]));
      //      printf("ma fonction : %d", ft_atoi(argv[1]));

    }
  return (0);
}
